import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tech.Diary',
  appName: 'Diary',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
